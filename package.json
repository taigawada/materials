{
    "name": "simple-education-dev",
    "license": "UNLICENSED",
    "private": false,
    "scripts": {
        "build": "npm run build --workspaces --if-present"
    },
    "workspaces": [
        "components",
        "icons",
        "tokens"
    ],
    "devDependencies": {
        "vite": "^3.0.6",
        "vue-tsc": "^0.40.1"
    }
}
